# キャットフード分析比較アプリ（Streamlit）

キャットフードを複数登録し、栄養素の比較（棒グラフ）、100gあたりのコスパ、総合スコアランキングを表示します。日本語UIです。

## 起動方法

```bash
pip install -r requirements.txt
streamlit run app.py
```

## データ保存

登録内容は `foods.json`（`app.py` と同じフォルダ）に保存されます。

